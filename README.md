# quiet-notes-server

## Recent update

A small cleanup pass on configuration handling and startup docs.
The app now reports missing values a bit more clearly and uses simpler defaults for local runs.

```bash
npm run dev
```

### Notes
- trimmed a few unused options from the example config
- clarified the local setup step for `huonghu`
- kept behavior the same for existing installs
